# Infrastructure

Local infrastructure is defined in the root `docker-compose.yml`.

V1 services:

- PostgreSQL
- Redis
- FastAPI backend
- Celery worker
- Next.js frontend

Do not add Kubernetes or other orchestration for V1.
